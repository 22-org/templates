{
    "template": "Dynamic template - variables are extracted from the template string itself",
    "description": "The template serves as a flexible schema where {variable_names} in the template string define the expected fields in sequence_data",
    "example_usage": {
        "template": "Complete the query '{partial_query}' based on {previous_searches} and interest in {user_category_preference}",
        "sequence_data": {
            "partial_query": "lap",
            "previous_searches": [
                "laptop",
                "tablet"
            ],
            "user_category_preference": "electronics"
        }
    },
    "output_fields": [
        "recommended_products",
        "relevance_scores",
        "ranking_reasons"
    ]
}